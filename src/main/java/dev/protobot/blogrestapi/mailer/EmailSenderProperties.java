package dev.protobot.blogrestapi.mailer;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

/*
@Component
public class EmailSenderProperties {

    private static final String MAIL_SMTP_HOST = "spring.mail.host";
    private static final String MAIL_SMTP_PORT = "spring.mail.port";

    @Value("${" + EmailSenderProperties.MAIL_SMTP_HOST + "}")
    private String smtpHost;

    @Value("${" + EmailSenderProperties.MAIL_SMTP_PORT + "}")
    private String smtpPort;

    public String getSmtpHost() {
        return smtpHost;
    }

    public int getSmtpPort() {
        return Integer.parseInt(this.smtpPort);
    }
}

 */
